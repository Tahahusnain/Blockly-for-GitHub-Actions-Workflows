{
  "type": "object",
  "required": ["name", "on", "jobs"],
  "properties": {
    "name": { "type": "string" },
    "env": { "type": "object" },
    "on": {
      "type": "object",
      "minProperties": 1,
      "properties": {
        "push": {
          "type": "object",
          "properties": {
            "branches": { "type": "array", "items": { "type": "string" } }
          }
        },
        "pull_request": {
          "type": "object",
          "properties": {
            "branches": { "type": "array", "items": { "type": "string" } }
          }
        },
        "workflow_dispatch": { "type": "null" },
        "schedule": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["cron"],
            "properties": { "cron": { "type": "string" } }
          }
        }
      }
    },
    "jobs": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["runs-on", "steps"],
        "properties": {
          "runs-on": { "type": "string" },
          "needs": { "type": "string" },
          "if": { "type": "string" },
          "environment": { "type": "string" },
          "env": { "type": "object" },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "oneOf": [
                {
                  "required": ["uses"],
                  "properties": {
                    "name": { "type": "string" },
                    "uses": { "type": "string" },
                    "if": { "type": "string" },
                    "with": { "type": "object" },
                    "env": { "type": "object" }
                  }
                },
                {
                  "required": ["run"],
                  "properties": {
                    "name": { "type": "string" },
                    "run": { "type": "string" },
                    "if": { "type": "string" },
                    "env": { "type": "object" }
                  }
                }
              ]
            }
          }
        }
      }
    }
  }
}
